import React from 'react'
import cls from 'classnames'
import s from './Main.module.scss'
import { Button, Input } from 'components'

const Main: React.FC = () => {
    return <main className={s.wrapper}>Авторизирован</main>
}

export default Main
